const FilesBar : React.FC = () => {

    return (
        <section className="sidebar">
            <button>HTML</button>
            <button>CSS</button>
            <button>JS</button>
        </section>
    )
}

export default FilesBar;